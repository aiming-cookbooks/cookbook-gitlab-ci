defaults: &defaults
  allowed_gitlab_urls: 
  <% @gitlab_hosts.each do |host| %>
    - <%= host %>
  <% end %>

development:
  <<: *defaults
  neat_setting: 800

test:
  <<: *defaults

production:
  <<: *defaults
